# E-Puck2 firmware fork for the university of Fribourg
## Dependencies 
To build the project, you will need the following packages:

- `make` 
- `gcc-arm-none-eabi`

```sh
sudo apt install make gcc-arm-none-eabi
```